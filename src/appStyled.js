import styled from "styled-components";

const AppWrapper = styled.div`
  margin: 0;
  display: flex;
  flex-direction: column;
  min-height: 100vh;
`
const FlexBox = styled.div`
  display: flex;
`
export { AppWrapper, FlexBox }